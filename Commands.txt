1) to inislize
	git init

2) check status
	git status

3) add files form working space to staging area
	git add <file_name>
  
    to add all files
        git add -A
	git add *.*
	git add .
		{all 3 are same}

4) adding files to local repo from staging area
	git commit -m "<comment>"



5) configring github in local repo.
	git remote add origin "https://github.com/...."

6) pusing files local to remote repo.
	git push -u origie master

7) see logs
	git log





Branching and Marging
=======================
1) Creating branch
	git branch "<branch_name>"

2) change branch to branch
	git checkout "trager-branch-name"